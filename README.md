# be-productive

productivity app

# how to run

```
    python3 -m venv .venv
    . .venv/bin/activate
    pip install -e .
    flask --app flaskr run --debug
```

# testing

```
##run test
pytest -v
coverage run -m pytest
```
